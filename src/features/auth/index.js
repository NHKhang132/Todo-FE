import React from 'react';
import { Outlet, Route, Routes } from 'react-router-dom';

const IndexAuth = () => {
    return(
        <div>
            <Routes>
                <Route path="/*" element={}></Route>
            </Routes>
        </div>
    )
}